Transient — каждый раз создаётся новый экземпляр сервиса.

Scoped — один экземпляр создаётся на время одного HTTP-запроса.

Singleton — создаётся один экземпляр на всё время работы приложения и используется всеми запросами.